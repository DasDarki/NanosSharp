namespace NanosSharp.Server;

/// <summary>
/// An internal class for event registration and management. It hooks the native Event system and wraps a C# layer
/// around it allowing both attribute-marked registration and the C# native event keyword.
/// </summary>
internal class EventRegistry
{
    
}