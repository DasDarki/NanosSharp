// Autogenerated by the NanosSharp Server Bindings Generator (c) 2022 DasDarki / GPLv3

using NanosSharp.API;

namespace NanosSharp.Server.Bindings;

public class TextRender : Paintable
{
    public static void SetColor(ILuaVM vm, LuaRef selfRef, LuaRef color)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "TextRender");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetColor");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, color);
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

    public static void SetFont(ILuaVM vm, LuaRef selfRef, FontType font_type)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "TextRender");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetFont");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        pc++;
        vm.PushEnum(font_type);
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

    public static void SetFreeze(ILuaVM vm, LuaRef selfRef, bool freeze)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "TextRender");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetFreeze");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        pc++;
        vm.PushBoolean(freeze);
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

    public static void SetGlyphSettings(ILuaVM vm, LuaRef selfRef, LuaRef? extrude = null, LuaRef? level = null, TextRenderBevelType? bevel_type = null, LuaRef? bevel_segments = null, bool? outline = null)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "TextRender");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetGlyphSettings");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        if (extrude != null)
        {
             pc++;
             vm.RawGetI(ILuaVM.RegistryIndex, extrude.Value);
        }
        if (level != null)
        {
             pc++;
             vm.RawGetI(ILuaVM.RegistryIndex, level.Value);
        }
        if (bevel_type != null)
        {
             pc++;
             vm.PushEnum(bevel_type.Value);
        }
        if (bevel_segments != null)
        {
             pc++;
             vm.RawGetI(ILuaVM.RegistryIndex, bevel_segments.Value);
        }
        if (outline != null)
        {
             pc++;
             vm.PushBoolean(outline.Value);
        }
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

    public static void SetMaxSize(ILuaVM vm, LuaRef selfRef, LuaRef? max_width = null, LuaRef? max_height = null, bool? scale_proportionally = null)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "TextRender");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetMaxSize");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        if (max_width != null)
        {
             pc++;
             vm.RawGetI(ILuaVM.RegistryIndex, max_width.Value);
        }
        if (max_height != null)
        {
             pc++;
             vm.RawGetI(ILuaVM.RegistryIndex, max_height.Value);
        }
        if (scale_proportionally != null)
        {
             pc++;
             vm.PushBoolean(scale_proportionally.Value);
        }
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

    public static void SetText(ILuaVM vm, LuaRef selfRef, string text)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "TextRender");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetText");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        pc++;
        vm.PushString(text);
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

    public static void SetTextSettings(ILuaVM vm, LuaRef selfRef, LuaRef? kerning = null, LuaRef? line_spacing = null, LuaRef? word_spacing = null, TextRenderHorizontalAlignment? horizontal_alignment = null, TextRenderVerticalAlignment? vertical_alignment = null)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "TextRender");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetTextSettings");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        if (kerning != null)
        {
             pc++;
             vm.RawGetI(ILuaVM.RegistryIndex, kerning.Value);
        }
        if (line_spacing != null)
        {
             pc++;
             vm.RawGetI(ILuaVM.RegistryIndex, line_spacing.Value);
        }
        if (word_spacing != null)
        {
             pc++;
             vm.RawGetI(ILuaVM.RegistryIndex, word_spacing.Value);
        }
        if (horizontal_alignment != null)
        {
             pc++;
             vm.PushEnum(horizontal_alignment.Value);
        }
        if (vertical_alignment != null)
        {
             pc++;
             vm.PushEnum(vertical_alignment.Value);
        }
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

}

