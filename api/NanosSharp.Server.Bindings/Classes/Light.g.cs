// Autogenerated by the NanosSharp Server Bindings Generator (c) 2022 DasDarki / GPLv3

using NanosSharp.API;

namespace NanosSharp.Server.Bindings;

public class Light : Actor
{
    public static void SetColor(ILuaVM vm, int selfRef, int color)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "Light");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetColor");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, color);
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

    public static void SetTextureLightProfile(ILuaVM vm, int selfRef, int light_profile)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "Light");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetTextureLightProfile");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, light_profile);
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

    public static void SetCastShadows(ILuaVM vm, int selfRef, bool is_shadows_enabled)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "Light");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetCastShadows");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        pc++;
        vm.PushBoolean(is_shadows_enabled);
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

    public static void SetIntensity(ILuaVM vm, int selfRef, double intensity)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "Light");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetIntensity");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        pc++;
        vm.PushNumber(intensity);
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

    public static void SetAttenuationRadius(ILuaVM vm, int selfRef, double attenuation_radius)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "Light");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "SetAttenuationRadius");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        pc++;
        vm.PushNumber(attenuation_radius);
        vm.MCall(pc, 0);
        vm.ClearStack();
    }

    public static bool GetCastShadows(ILuaVM vm, int selfRef)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "Light");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "GetCastShadows");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        vm.MCall(pc, 1);
        var r0 = vm.ToBoolean(-1);
        vm.Pop();
        vm.ClearStack();
        return r0;
    }

    public static double GetIntensity(ILuaVM vm, int selfRef)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "Light");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "GetIntensity");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        vm.MCall(pc, 1);
        var r0 = vm.ToNumber(-1);
        vm.Pop();
        vm.ClearStack();
        return r0;
    }

    public static double GetAttenuationRadius(ILuaVM vm, int selfRef)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "Light");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "GetAttenuationRadius");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        vm.MCall(pc, 1);
        var r0 = vm.ToNumber(-1);
        vm.Pop();
        vm.ClearStack();
        return r0;
    }

    public static int GetColor(ILuaVM vm, int selfRef)
    {
        int pc = 0;
        vm.PushGlobalTable();
        vm.GetField(-1, "Light");
        vm.GetField(-1, "__function");
        vm.GetField(-1, "GetColor");
        pc++;
        vm.RawGetI(ILuaVM.RegistryIndex, selfRef);
        vm.MCall(pc, 1);
        var r0 = vm.Ref(ILuaVM.RegistryIndex);
        vm.ClearStack();
        return r0;
    }

}

